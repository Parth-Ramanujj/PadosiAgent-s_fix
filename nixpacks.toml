[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader", "php artisan storage:link || true", "php artisan config:cache", "php artisan route:cache", "php artisan view:cache"]

[start]
cmd = "php artisan storage:link || true ; php artisan serve --host=0.0.0.0 --port=$PORT"
